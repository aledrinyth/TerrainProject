// import firebase-app and firebase-firestore packages when we get firebase


var config; // All this copy paste from the firebase when we get it (put API key in an env and use dotenv)

const db = firebase.firestore();